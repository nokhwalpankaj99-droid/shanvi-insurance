PI upload RLS fix

1. In Supabase SQL Editor, run supabase_pi_setup.sql (or just rerun the storage policy section).
2. The website now uploads with POST without x-upsert, so only INSERT permission is required.
3. Bucket name: INSURANCE.
4. Do not send or use the Supabase secret key in the browser.
